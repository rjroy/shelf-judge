---
title: "Implementation notes: trusted collection insights consumers"
date: 2026-08-27
status: complete
tags: [implementation, notes, trusted-insights, profile]
source: .lore/specs/collection/collection-profiling.md
modules: [shared, daemon, cli, web]
---

# Implementation notes: trusted collection insights consumers

## Progress

### Phase checklist

- [x] Phase 1: inspect existing trusted-insight types, schemas, engine behavior, persistence, and prior implementations
- [x] Phase 1: complete the shared axis-suggestion contract and runtime validation
- [x] Phase 1: emit deterministic reported, insufficient, suppressed, retired, and empty engine outcomes
- [x] Phase 1: version and validate persisted profile reloads
- [x] Phase 1: focused verification and review
- [x] Phase 1 review correction: constrain suggestion method identities and versions by status
- [x] Phase 1 review correction: validate insight cohort arithmetic, coverage, and profile bounds
- [x] Phase 1 review correction: preserve inclusive coverage tolerance at floating-point boundaries
- [x] Phase 2: verify daemon profile route pass-through with runtime schema validation
- [x] Phase 2: verify CLI client and command preserve complete nested insight JSON
- [x] Phase 2: cover reported, insufficient, suppressed, retired, and empty transport states
- [x] Phase 2 review correction: cover reported and insufficient divergence/outlier transport
- [x] Phase 2 review correction: distinguish evaluated-empty arrays from unavailable divergence
- [x] Phase 3: build one reusable trusted-insight presentation for overview and game detail
- [x] Phase 3: render reported evidence and explicit insufficient/suppressed/retired states
- [x] Phase 3: distinguish evaluated-empty and unavailable families without hiding sections
- [x] Phase 3: associate game-detail insights through stable IDs, evidence, and comparators
- [x] Phase 3: remove UI credibility thresholds and keep suggestions question-framed
- [x] Phase 3: add responsive desktop/mobile presentation and focused tests
- [x] Acceptance correction: distinguish profile transport failure from an empty collection
- [x] Acceptance correction: preserve family-level abstentions on game detail
- [x] Acceptance correction: validate web and CLI profile responses at client boundaries
- [x] Acceptance correction: enforce reported divergence cross-field invariants
- [x] Acceptance correction: add semantic heading and ARIA relationships
- [x] Acceptance correction: replace source-only consumer claims with rendered integration coverage
- [x] Acceptance correction: complete final focused verification
- [x] Correction round 2: enforce strict reported-divergence score and threshold ordering
- [x] Correction round 2: distinguish game-scoped abstentions from family-scoped abstentions
- [x] Correction round 2: retain the documented no-browser residual without claiming viewport proof
- [x] Correction round 3: align reported divergence TypeScript and runtime notability contracts
- [x] Terminal acceptance: align suggestion/outlier formulas, runtime schemas, and reported types
- [x] Historical terminal acceptance: repair the stale-v4/then-current-v5 derived-axis integration lifecycle
- [x] Terminal acceptance: complete repository-wide verification
- [x] Terminal acceptance R1: gate suggestion reporting on the published rounded effect
- [x] Independent acceptance review

## Obligations And Evidence

| Obligation                                                                                            | Phase    | Evidence                                                                        |
| ----------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------- |
| Complete the shared trusted profile insight contract without changing divergence/outlier behavior     | Phase 1  | Shared type/schema tests and existing divergence/outlier engine tests           |
| Distinguish reported, insufficient, suppressed, retired, and successful-empty states where applicable | Phase 1  | Shared union/schema tests plus deterministic suggestion-engine status tests     |
| Emit explicit insufficiency for inadequate suggestion prerequisites and samples                       | Phase 1  | Missing Tournament/BGG coverage and undersized support/comparator engine tests  |
| Explicitly retire legacy concentration and variance recommendation methods                            | Phase 1  | Legacy-trigger fixtures asserting retired method records                        |
| Keep retained suggestions question-framed by contract                                                 | Phase 1  | Required reported interpretation type and runtime question validation           |
| Reject malformed and non-finite persisted shapes                                                      | Phase 1  | Shared schema rejection tests and persistence invalidation tests                |
| Persist and reload all new shapes under a new profile version                                         | Phase 1  | Storage reload test containing reported, insufficient, and retired suggestions  |
| Reserve `[]` for successful evaluation with nothing notable                                           | Phase 1  | Adequate null-effect engine fixture asserting an empty array                    |
| Do not modify API, CLI, or web consumers                                                              | Phase 1  | Changed-file manifest audit                                                     |
| Bind current and retired suggestion statuses to their supported method IDs and versions               | Phase 1  | Shared type/schema rejection tests and persisted-artifact invalidation tests    |
| Reject contradictory or profile-impossible insight cohorts across every family                        | Phase 1  | Shared family-boundary tests and persistence invalidation tests                 |
| Transport the complete trusted union through `GET /api/profile` without projection                    | Phase 2  | Current-v7 cached-profile route test, schema parse, and exact nested equality   |
| Transport the complete trusted union through the CLI client and `profile` command                     | Phase 2  | Client and command tests using one shared typed fixture and exact JSON equality |
| Preserve explicit empty insight families through CLI output                                           | Phase 2  | Empty-profile command fixture and schema-backed equality assertion              |
| Preserve divergence and outlier family-specific reported/insufficient payloads through transport      | Phase 2  | Expanded shared fixture plus route/client/command exact-equality tests          |
| Represent evaluated-empty as three empty arrays on a nonempty collection                              | Phase 2  | CLI schema parse and explicit divergence/outlier/suggestion array assertions    |
| Render full trusted records consistently on overview and game detail                                  | Phase 3  | Shared renderer used by all three family wrappers on both pages                 |
| Expose evidence, comparison games, sufficiency, limitations, and notability without confidence claims | Phase 3  | Component render tests against the shared trusted transport fixture             |
| Keep abstained-only and evaluated-empty sections visible and distinct from unavailable                | Phase 3  | Explicit status and empty/null component tests                                  |
| Avoid UI statistical thresholds and preserve question framing                                         | Phase 3  | Production source audit and retained-suggestion render assertions               |
| Keep evidence and comparison games inspectable                                                        | Phase 3  | Linked evidence/comparator render tests on overview and game detail             |
| Distinguish profile load failure from empty collection/evaluation                                     | Review   | Controlled-loader overview and game-detail integration tests                    |
| Preserve collection/method-level abstentions when filtering for a game                                | Review   | Exact `outlier:collection` and method-level suggestion detail tests             |
| Reject malformed or unsupported profile responses in web and CLI clients                              | Review   | Injected web request and CLI fetch-boundary rejection tests                     |
| Reject contradictory reported divergence fields                                                       | Review   | Stable identity, evidence, score, sufficiency, provisional, notability tests    |
| Provide semantic section/card heading relationships                                                   | Review   | Rendered `h1`/`h2`/`h3` and `aria-labelledby` assertions                        |
| Reject zero/equal-score or non-strict-threshold reported divergence                                   | Review 2 | Equal, zero-gap, exact-threshold, below-threshold, and floating-point tests     |
| Keep game-scoped abstentions off unrelated detail pages                                               | Review 2 | Positive/negative `outlier:game-6` and family abstention filtering tests        |
| Require reported divergence threshold/direction statically and at runtime                             | Review 3 | Specialized type assertions, schema literals, and fixture parse regression      |
| Reject contradictory or non-notable reported suggestions/outliers                                     | Terminal | Formula, direction, threshold, type, fixture, floating-point, producer tests    |
| At the historical terminal milestone, invalidate v4 during migration and reload then-current v5       | Terminal | End-to-end stale discard, recompute, version, content, and no-rewrite checks    |

## Log

The log preserves the versions that were current when each milestone ran. Algorithm v5 and v6 references below are historical. The current expectation is profile contract v6 and algorithm v7; algorithm-v5 and algorithm-v6 caches are discarded rather than treated as compatible.

- 2026-08-27: Inspected the existing generic trusted-insight contract, family-specific schemas, profile engine, storage validation, profile version constants, tests, and pre-replacement suggestion implementation.
- 2026-08-27: Found that generic types already model all four statuses, but `AxisSuggestion`, its runtime schema, and engine output narrow the family to reported records and collapse every abstention into `[]`.
- 2026-08-27: Chose trigger-based retirement records for the two legacy recommendation methods. This exposes retirement when an old method would have recommended an axis while preserving `[]` for a completed current-method evaluation with no notable result.
- 2026-08-27: Consumer changes are intentionally deferred by phase scope.
- 2026-08-27: Added `ReportedAxisSuggestion` with a required question-framed interpretation and widened `AxisSuggestion` to the complete trusted union. Runtime validation accepts all discriminated states, enforces the reported question, and rejects malformed reasons, structures, and non-finite numbers.
- 2026-08-27: The current directional method now emits method-level insufficiency for missing BGG/Tournament coverage or fewer than six evaluated games, candidate-level insufficiency for undersized or absent comparison groups, and suppression for confounded candidates. Existing divergence and outlier implementations were not changed.
- 2026-08-27: Legacy concentration and variance checks now emit deterministic retired records only when their old thresholds would have recommended an uncovered axis. Adequate current-method evaluation with no reportable effect remains `[]`.
- 2026-08-27: At this then-current phase-1 milestone, advanced profile contract and algorithm versions from 4 to 5. Persistence tests pinned invalidation of immediate version 4 artifacts, reloaded all four suggestion statuses, preserved the empty profile case, and discarded non-finite insight data.
- 2026-08-27: Initial focused test run passed 57 tests and failed five stale `[]` assertions. Initial daemon typecheck failed five corresponding un-narrowed union accesses; shared typecheck passed. Tests and narrowing were updated rather than weakening the contract.
- 2026-08-27: Second focused run passed 66 tests; shared and daemon typechecks passed. A subsequent focused lint run found four unsafe matcher-array assertions, which were replaced with typed direct assertions.
- 2026-08-27: Final focused validation passed 182 tests with 469 assertions across profile engine/service, storage/persistence, shared trusted contracts, and shared validation. Shared and daemon typechecks, focused ESLint, focused Prettier, and `git diff --check` all passed.
- 2026-08-27: Final review found no remaining phase-1 contract, engine, persistence, or test issues. API, CLI, and web consumers remain intentionally untouched for later phases.
- 2026-08-27: Review finding SJ6KC3-001 identified that axis-suggestion statuses accepted arbitrary method metadata. Added literal current and retired method types, bound reported/insufficient/suppressed schemas to `directional-divergence-attribute-effect` version 1, and bound retired schemas to `unexpressed-concentration` or `high-variance` version 1. Shared and persistence tests reject cross-status IDs and unsupported versions in both directions.
- 2026-08-27: Review finding SJ6KC3-002 identified unchecked cohort arithmetic and a persisted six-game fixture paired with `profile.gameCount: 0`. Added one shared cohort schema requiring included plus excluded to equal eligible, exact zero coverage for zero eligible games, and nonzero coverage within 0.05 percentage points of included/eligible. `CollectionProfileSchema` now bounds every divergence, outlier, and suggestion cohort count by `profile.gameCount` without inferring counts from evidence arrays.
- 2026-08-27: Corrected the persistence fixture to declare six profile games. Existing engine producers passed the new invariants without changes. Review-correction validation passed 185 focused tests with 496 assertions, shared and daemon typechecks, focused ESLint, focused Prettier, and `git diff --check`.
- 2026-08-27: Remaining SJ6KC3-002 review found that binary subtraction could place a mathematically exact 0.05 percentage-point difference just above the inclusive boundary. The comparison now permits only scale-aware `Number.EPSILON` representation error beyond the unchanged 0.05 contract. Shared regression coverage accepts eligible 80, included 1, coverage 1.3 and rejects a value 1e-12 beyond that boundary. Web consumer errors remain deferred to phase 3.
- 2026-08-27: Floating-point boundary correction validation passed 86 focused shared tests with 162 assertions, shared typechecking, focused ESLint, focused Prettier, and `git diff --check`.
- 2026-08-27: A second SJ6KC3-002 correction found that one scale-relative epsilon did not cover accumulated error for included 339 of eligible 2000, where expected coverage evaluates to 16.950000000000003. The inclusive comparison now uses a conventional four-ULP scale-relative error bound for the division, multiplication, and two subtraction operations while retaining the exact 0.05 contractual tolerance. Regression cases cover both 1 of 80 at 1.3 and 339 of 2000 at 16.9; moving either value 1e-12 farther from expected coverage remains invalid.
- 2026-08-27: The first second-correction test run had 85 passing and one failing assertion because the 339-of-2000 outside fixture added 1e-12 toward expected coverage. Corrected the test to move each rounded boundary value away from expected coverage. Final validation passed 86 shared tests with 164 assertions, shared typechecking, focused ESLint, focused Prettier, and `git diff --check`.
- 2026-08-27: Phase 2 inspection confirmed production transport was already pass-through: the daemon route returns the service `CollectionProfile`, the CLI client returns response data, and the profile command only serializes that object. No production DTO, projection, or transport code was added.
- 2026-08-27: Added one shared typed transport fixture containing reported, insufficient, suppressed, and retired suggestions with complete nested method, cohort, sufficiency, evidence, comparator, limitations, observation, interpretation, notability, and confidence fields. Added a separate empty-family projection from the same fixture.
- 2026-08-27: At the phase-2 milestone, the daemon route test persisted the fixture under the then-current profile contract and algorithm v5, retrieved it through the real profile service and `GET /api/profile`, parsed the actual response with `CollectionProfileSchema`, and asserted exact equality. The retained test now follows the current contract-v6/algorithm-v7 constants; this historical entry does not claim later algorithms existed during phase 2.
- 2026-08-27: CLI client and command tests assert exact nested equality after HTTP JSON parsing and command JSON serialization, including explicit empty suggestions. Initial runtime tests passed, while typechecks exposed test-only Zod tuple inference and Bun fetch typing; explicit public-contract widening and a typed fetch seam resolved them. The first lint run found the root fixture outside a TypeScript project, so it moved under shared test fixtures without duplication or relaxed linting.
- 2026-08-27: Final phase 2 validation passed 14 focused daemon route and CLI transport tests with 50 assertions; shared, daemon, and CLI typechecks; focused ESLint and Prettier; and `git diff --check`.
- 2026-08-27: Review finding SJ6KC3-P2-001 identified that phase 2 transport coverage exercised the complete suggestion union but omitted divergence and outlier family payloads. Expanded the shared fixture with schema-valid reported and insufficient divergence records and reported and insufficient outlier records, including family details and complete trusted evidence fields where applicable. Every cohort remains bounded by the six-game profile.
- 2026-08-27: Review finding SJ6KC3-P2-002 identified that the empty fixture used `divergence: null` and `gameCount: 0`, conflating unavailable analysis with completed evaluation that found nothing notable. The evaluated-empty fixture now retains six games and explicitly sets divergence, outliers, and suggestions to empty arrays; the CLI test asserts all three after serialization.
- 2026-08-27: Phase 2 review-correction validation passed 100 focused shared validation, daemon route, and CLI transport tests with 222 assertions; shared, daemon, and CLI typechecks; focused ESLint and Prettier; and `git diff --check`.
- 2026-08-27: Phase 3 replaced three reported-only profile projections and duplicated game-detail summaries with one `TrustedInsightSection` renderer that receives complete divergence, outlier, or suggestion union objects. Thin family wrappers provide only titles, empty/unavailable language, game filtering, and optional suggestion dismissal.
- 2026-08-27: At the phase-3 milestone, the shared cards rendered the generic contract's optional confidence branch alongside observation, interpretation/question, notability, method/cohort metadata, sufficiency gates, evidence, comparators, limitations, and abstention explanations. Current reported insight types later narrowed confidence to `null`, and the unreachable rendering branch was removed under SJ6KC7-VR-003.
- 2026-08-27: Profile Overview now always renders all three families. Divergence `null` renders unavailable, while empty arrays render evaluated with nothing notable; abstained-only arrays remain visible. Suggestions use non-prescriptive question labels and preserve the contract's question wording.
- 2026-08-27: Game detail now passes full family arrays to the same wrappers. Association uses stable `divergence:`/`outlier:` IDs, evidence game IDs, and comparator IDs, so abstentions without `details` and evidence-backed suggestions remain discoverable. The old details-only divergence/outlier rendering was removed.
- 2026-08-27: Removed the hard-coded `gap > 1.5 pts` UI claim. The renderer displays only contract-owned notability explanations and performs no credibility filtering. Added responsive evidence grids, stacked metadata/assessment cards, mobile measurement rows, and shared responsive structure markers using existing profile breakpoints and tokens.
- 2026-08-27: Phase 3 focused validation passed 35 profile/game-link/responsive component tests with 118 assertions, production web typechecking, focused ESLint, focused Prettier, and `git diff --check`. The broader test-only TypeScript project still reports pre-existing unrelated fixture errors in axes, score-breakdown, and shelf-assignment tests; no phase 3 production or focused-test type error remains.
- 2026-08-27: Reopened `shelf-judge-6kc.3` after acceptance review found seven unresolved items. The issue remains in progress for independent re-review and must not be closed by implementation.
- 2026-08-27: Resolved SJ6KC3-P3-001 by parsing profile load outcomes into explicit loaded/unavailable states. Overview now reserves the add-games prompt for a successfully loaded zero-game profile; transport or validation failures render `Profile analysis unavailable`. Game detail always renders divergence, outlier, and suggestion sections, using `null` for every family after failed profile loading so all three report unavailable rather than disappearing.
- 2026-08-27: Resolved SJ6KC3-P3-002 by treating abstained records without game evidence as family-level states during game filtering. Exact rendered tests retain `outlier:collection` and the method-level `axis-suggestion:insufficient` record on an unrelated game rather than converting either family to evaluated-empty; suppressed and retired method states remain visible as well.
- 2026-08-27: Resolved SJ6KC3-CONS-003 by applying `CollectionProfileSchema.safeParse` to successful profile and narration responses in both production web and CLI clients. Malformed shapes and unsupported nested insight contract versions now reject with `Invalid profile response`; CLI narration errors retain the existing status/error payload convention without an unsafe error assertion.
- 2026-08-27: Resolved SJ6KC3-CONTRACT-004 after checking `computeDivergence`. Reported divergence validation now binds stable ID, exactly one subject ID/name, the same-subject independent comparator, absolute gap arithmetic, score direction, comparison-count sufficiency, non-provisional status, and notability value. Ten focused mismatch cases reject independently, while the shared persisted transport fixture and daemon producer/persistence/route tests remain valid.
- 2026-08-27: Resolved SJ6KC3-A11Y-005 with page `h1`, section `h2`, card `h3`, and subsection `h4` hierarchy. Trusted sections and articles use deterministic `aria-labelledby` relationships while retaining subject and comparator links.
- 2026-08-27: Resolved SJ6KC3-TEST-006 using the repository's strongest available setup. No browser/E2E runner is installed, so controlled profile loaders and page-owned overview/detail surfaces now exercise unavailable, zero-game, retained, evaluated-empty, insufficient, suppressed, and retired states with links and headings. Responsive tests retain DOM/CSS assertions and now pin card `min-width: 0` plus `overflow-wrap: anywhere`; residual risk is the absence of layout-engine viewport, horizontal-overflow, and screenshot validation.
- 2026-08-27: Resolved SJ6KC3-LORE-007 by listing shared, daemon, CLI, and web modules in frontmatter and replacing stale later-consumer work with the acceptance-correction checklist.
- 2026-08-27: Acceptance-correction verification passed 178 focused shared/daemon/CLI tests with 446 assertions and 45 focused web tests with 157 assertions. Root shared/daemon/CLI typechecking, production web typechecking, focused ESLint, changed-file Prettier, `git diff --check`, and the Next production build all pass. Repository-wide ESLint remains externally blocked by 14 unrelated ignored files under `tmp/HEPCAT`; the affected project source set is clean.
- 2026-08-27: Correction round 2 finding SJ6KC3-VERIFY-001 identified that arithmetic consistency alone still admitted equal-score, zero-gap records and did not require reported notability to exceed its threshold. `ReportedTournamentDivergenceSchema` now requires a declared threshold, strictly different score ordering, positive gap, matching direction, `above` notability direction, and both the arithmetic gap and notability value strictly above the threshold. Four-ULP scale-relative equality remains limited to arithmetic consistency, matching the existing insight floating-point convention; strict threshold comparison remains direct because the producer itself uses `gap > threshold`.
- 2026-08-27: Round 2 contract tests independently reject equal scores, zero gap, gap exactly at threshold, and gap below threshold. A `0.3 - 0.1` case remains accepted against a persisted `0.2` gap to pin representation tolerance, while the persisted transport fixture and real daemon producer, route, and persistence suites remain valid.
- 2026-08-27: Correction round 2 finding SJ6KC3-VERIFY-002 identified that the generic evidence-empty fallback incorrectly treated stable `outlier:<gameId>` abstentions as collection-wide. Producer IDs show divergence and game outliers are game-scoped, `outlier:collection` is the sole collection-level outlier, and abstained suggestions are method/candidate records rather than game records. Divergence now filters only by stable ID/evidence/comparator, outliers explicitly retain only `outlier:collection` as family scope, and suggestions retain evidence-empty non-reported method/candidate states within the suggestion wrapper. Tests show `outlier:game-6` on game 6 and evaluated-empty on an unrelated game, while retaining `outlier:collection` and method-level suggestion insufficiency on unrelated details.
- 2026-08-27: SJ6KC3-VERIFY-003 remains an accepted limitation: this repository has no browser/E2E runner, and no framework was added solely for this task. Rendered consumer integration, semantic DOM, mobile CSS, `min-width`, and overflow-wrap safeguards remain covered, but notes and handoff do not claim real viewport, horizontal-overflow, or screenshot proof.
- 2026-08-27: Correction round 2 validation passed 91 focused shared/daemon tests with 288 assertions and 38 focused web tests with 142 assertions. Root shared/daemon/CLI typechecking, production web typechecking, focused ESLint, changed-file Prettier, `git diff --check`, and the Next production build all pass. The Beads issue remains in progress for independent acceptance review.
- 2026-08-27: Correction round 3 finding SJ6KC3-R2-001 identified that `TournamentDivergenceInsight` still exposed reported notability through generic `InsightNotability`, permitting `threshold: null` and non-`above` directions at compile time despite stricter runtime validation. Added exported `ReportedTournamentDivergence`, preserving every generic `ReportedInsight<TournamentDivergenceDetails>` field while specializing only notability threshold to `number` and direction to `"above"`; the existing union and consumer ergonomics remain unchanged.
- 2026-08-27: The reported divergence Zod schema now uses the same required numeric threshold and literal `"above"` direction, so parsed web/CLI data is assignable to the public type without assertions. Compile-time tests pin the reported union member, both narrowed fields, and equality of all non-notability fields with the generic reported insight. The persisted transport fixture is parsed as a runtime compatibility regression.
- 2026-08-27: Correction round 3 validation passed 117 focused shared/daemon/CLI/web tests with 386 assertions. Shared, daemon, CLI, and production web typechecks, focused ESLint, changed-file Prettier, and `git diff --check` pass. The Beads issue remains in progress.
- 2026-08-27: The user authorized continuing beyond the correction limit to resolve terminal acceptance blockers SJ6KC3-AR-001 and SJ6KC3-AR-002. The issue remains in progress until re-acceptance.
- 2026-08-27: For SJ6KC3-AR-001, inspected the actual producers before adding invariants. Suggestions calculate signed Tournament-minus-independent-fitness means, derive tournament-outlier effect as supporting minus comparator and fitness-outlier effect as comparator minus supporting, then independently round both means and effect to tenths. Runtime validation now requires the supporting mean sign to match direction and permits at most the producer's one-tenth independent-rounding difference, with the existing four-ULP scale-relative allowance at the inclusive boundary.
- 2026-08-27: Reported suggestion notability now requires a finite numeric threshold, literal `above` direction, effect/value consistency, and both effect and notability value strictly above the declared threshold. The initial producer correction changed the raw-effect gate from `<` to `<=`; that prevented an unrounded exact-threshold effect but did not prevent a raw value just above 1.5 from publishing as 1.5 after rounding. Tests reject contradictory means/effect/direction, exact/below thresholds, wrong direction, null threshold, and mismatched notability while accepting tournament and fitness directions, shared fixtures, real producer output, and decimal floating-point/rounding cases.
- 2026-08-27: Reported outlier notability now requires a finite numeric threshold, literal `above` direction, ULP-tolerant value/neighborhood equality, and strict threshold exceedance. Added exported `ReportedCollectionOutlier`; both it and `ReportedAxisSuggestion` expose `threshold: number` and `direction: "above"`, keeping public types aligned with parsed data. Tests reject exact/below thresholds, wrong direction, null threshold, and mismatched values while retaining shared and floating-point fixtures.
- 2026-08-27: For SJ6KC3-AR-002, the derived-axis end-to-end fixture intentionally remained profile contract/algorithm v4 so it exercised stale-cache invalidation. The test no longer falsely typed or expected that artifact as current `ProfileData`: it asserted v4 was discarded, performed collection migration and both repairs, recomputed through `GET /api/profile`, verified the persisted artifact used the then-current v5 constants, and reloaded identical content without rewriting either collection or profile data.
- 2026-08-27: Terminal acceptance validation passed 111 focused shared tests with 199 assertions, 79 initial focused daemon tests with 388 assertions, and the combined post-format focused gate with 190 tests and 587 assertions. Shared, daemon, CLI, and production web typechecks, focused ESLint, changed-file Prettier, and `git diff --check` pass. The full repository suite passes 1,909 tests with 1 skip and 7,235 assertions, and the Next production build passes. The Beads issue remains in progress for re-acceptance.
- 2026-08-27: SJ6KC3-AR-001-R1 moves all suggestion publication rounding immediately after raw mean/effect calculation, computes each rounded field exactly once, and makes the strict reporting decision against `roundedEffect`, the same value stored in both `details.effect` and `notability.value`. Deterministic producer controls cover raw effect 1.50, raw effect approximately 1.51 publishing as 1.5, and clearly notable effect 1.60 publishing as 1.6; each resulting suggestion array is validated through `CollectionProfileSchema`.
- 2026-08-27: AR-001-R1 verification passed 167 focused engine/shared/schema tests with 383 assertions. The full repository suite passes 1,911 tests with 1 skip and 7,241 assertions; shared, daemon, CLI, and production web typechecks, focused ESLint, changed-file Prettier, `git diff --check`, and the Next production build all pass. The Beads issue remains in progress.
- 2026-08-27: Independent acceptance completed with no remaining contract, transport, persistence, CLI, overview, game-detail, accessibility, or producer-consistency blockers. The implementation notes are complete; the issue may close.

## Accepted Validation

- Focused terminal regression: 167 tests passed with 383 assertions.
- Full repository suite: 1,911 passed, 1 skipped, 0 failed, with 7,241 assertions.
- Production typechecks: shared, daemon, CLI, and web passed.
- Focused changed-file ESLint and Prettier passed; `git diff --check` passed.
- Next production build passed.

## Known Limitations

- The repository has no browser/E2E runner. Rendered integration and responsive DOM/CSS safeguards pass, but acceptance does not claim real viewport, horizontal-overflow, or screenshot proof.
- Repository-wide `bun run lint` traverses unrelated ignored `tmp/HEPCAT` content and reports 14 project-service parser errors there. Focused lint for every accepted changed source/test path passes.
- Validation ran with Bun 1.3.11 while `package.json` declares `bun@1.4.0`; no observed failure is attributed to this version difference, but exact declared-runtime reproduction remains outside this acceptance run.

## Accepted Manifest

The exact post-close porcelain status, index blob identity, and working-tree SHA-256 for every changed or untracked accepted path are captured in the final acceptance handoff after all notes and Beads mutations, so the notes file and Beads bookkeeping entries reflect their final content rather than a pre-close snapshot.
